<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% if (key == 0) { %>
      <a href="/session/login">로그인</a>
      <a href="/session/join">회원가입</a>
      <a href="/session/list">회원목록</a>
    <% } else if(key == 1) { %> 
      <form action="/session/logout" method="post">
        <input type="submit" value="로그아웃" />
      </form>
    <% } %>
  </body>  
</html>
